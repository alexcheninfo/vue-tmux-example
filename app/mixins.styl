@import './variables'

/* Basic */

list-style($list-style) {
  list-style-type: $list-style
  margin: 0
  text-align: left
}

border-radius($radius) {
  -webkit-border-radius: $radius
  -moz-border-radius: $radius
  border-radius: $radius
}

box-shadow($position) {
  -webkit-box-shadow: $position $shadow-blur $shadow-color
  -moz-box-shadow: $position $blur $shadow-blur $shadow-color
  box-shadow: $position $shadow-blur $shadow-color
}

filter-drop-shadow($position) {
  -webkit-filter: drop-shadow($position $shadow-blur $shadow-color)
  -moz-filter: drop-shadow($position $shadow-blur $shadow-color)
  filter: drop-shadow($position $shadow-blur $shadow-color)
}

/* Hovers */

bob-hover($position) {
  &:hover {
    z-index: 9999
    -webkit-transform: translate($position)
    transform: translate($position)
  }
}

/* Transitions */

transition($duration) {
  -webkit-transition: all $duration ease-in-out
  transition: all $duration ease-in-out
}

v-transition($duration) {
  &.v-transition {
    -webkit-transition: all $duration ease
    transition: all $duration ease
  }
}

v-scale-transition($size) {
  &.v-enter, .v-leave {
    -webkit-transform: scale($size)
    transform: scale($size)
  }
}
